# 完成第二课：第一个后端接口

用户完成了第二课的全部内容：创建了 Hono 项目，写出了 `GET /` 和 `POST /api/chat`
两个接口，用 `test.http` 文件成功测试，并完成了三道测验。

项目目录：`E:\project\ai-backend`，已有 `src/index.ts` 和 `src/test.http`。

**Implications**：可以进入第三课——用 `@anthropic-ai/sdk` 把 `/api/chat` 里的
假回复替换成真正的 Claude 回复，同时引入 `.env` 管理 API Key。
