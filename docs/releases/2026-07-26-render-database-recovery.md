# 发布记录：Render 数据库恢复

## 部署标识与时间

- 服务：AI Agent 后端 Web Service
- 平台：Render
- 记录日期：2026-07-26
- 触发原因：免费数据库到期，导致服务无法完成数据库初始化。

## 变更摘要

替换过期的免费 Render 数据库，并更新服务连接配置后重新部署后端服务。旧数据库中的业务数据未迁移；本次恢复以服务可用性和新数据库初始化为目标。

## 验收证据

- 公网根路径返回服务运行状态。
- `GET /health` 返回 `200` 与 `ok`。
- `GET /ready` 返回 `200` 与 `ready`。
- 本地完整测试套件通过：2 个测试文件、9 条测试。

## 风险与限制

- 新免费数据库从空状态开始，不包含旧库数据。
- 免费实例可能再次到期，不适合作为长期生产数据持久化方案。
- 本次仅验证存活与就绪探针，未执行会写入数据或调用 AI 的业务烟雾测试。

## 回滚点

若新数据库连接或就绪检查再次失败，停止继续发布操作，并在 Render 中恢复到最近一次已验证可用的服务与数据库组合。回滚前先保留部署时间、探针结果和受控日志入口，避免用临时关闭鉴权或跳过就绪检查掩盖问题。

## 后续事项

- 在下一次发布前记录版本标识、变更摘要和探针结果。
- 为长期数据选择具备持久化和备份策略的数据库方案。
- 准备合成账号与只读业务路径后，再补充第三层业务烟雾测试。
